
# PostgreSQL Database config
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=asdfghjkl
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Properties
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# (Optional) If you want to use a connection pool, you can configure it like this (using HikariCP as an example)
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

# (Optional) Additional PostgreSQL settings
# spring.datasource.initialization-mode=always
# spring.datasource.platform=postgres
# spring.datasource.continue-on-error=true

# (Optional) Logging SQL queries
# spring.jpa.show-sql=true
# logging.level.org.hibernate.type.descriptor.sql=trace
